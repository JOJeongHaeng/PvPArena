# Codex Notes

- Prefer Blueprint for UI visual tuning such as size, padding, alignment, colors, fonts, and layout polish.
- Keep C++ focused on data binding, replication, logic, and default widget hookups.
- If a UI adjustment can be done cleanly in Blueprint, ask the user to make the editor-side change instead of hard-coding the visual tweak in C++.
